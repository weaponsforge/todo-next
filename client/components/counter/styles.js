const styles = {
  container: {
    marginTop: (theme) => theme.spacing(2),
    textAlign: 'center'
  },
  counterContainer: {
    marginTop: (theme) => theme.spacing(2),
  }
}

export default styles
